{
  "name": "api",
  "scripts": {
    "start": "wattpm start",
    "clean": "rm -fr ./dist",
    "build": "wattpm build",
    "migrate": "wattpm api:migrations:apply"
  },
  "type": "module",
  "devDependencies": {
    "fastify": "^5.0.0"
  },
  "dependencies": {
    "@platformatic/db": "^3.0.0",
    "@platformatic/service": "^3.5.1",
    "@platformatic/tsconfig": "^0.2.0"
  }
}
